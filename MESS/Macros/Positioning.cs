namespace MESS.Macros
{
    enum Positioning
    {
        /// <summary>
        /// Instance offsets are relative to the position of the parent macro entity.
        /// </summary>
        Relative,

        /// <summary>
        /// Instance offsets are treated as absolute positions.
        /// </summary>
        Absolute,
    }
}
